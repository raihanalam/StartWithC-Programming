#include<stdio.h>
int main()
{
    int i,min=100*100,arr[]={6,9,2,22,1,2,12,17,19,20};
    scanf("%d",&number);

    for(i=0;i<10;i++)
    {
        if(arr[i]<=min)
        {
            min=arr[i];
        }
    }
    printf("%d\n",count);

    return 0;
}
